This is a simple example on how to use the free GNU gcc compiler and the Make tool.

To compile the exame, type "make" in the terminal.
> make
gcc -o Hello -Wall -std=c11 main.c

To execture the compiled program, type:
> ./Hello
Hello world!
>

